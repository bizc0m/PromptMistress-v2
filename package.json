{
  "name": "promptmistress",
  "version": "0.2.0",
  "private": true,
  "description": "Fusion workspace for Prompt Vault variants without feature loss.",
  "type": "module",
  "scripts": {
    "start": "node scripts/server.mjs",
    "test": "node scripts/verify.mjs && node scripts/test-extension.mjs",
    "test:extension": "node scripts/test-extension.mjs",
    "build": "./scripts/build.sh"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "puppeteer-core": "^23.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
